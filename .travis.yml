language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-goat' ]; then cd .. && eval "mv $currentfolder generator-goat" && cd generator-goat; fi
